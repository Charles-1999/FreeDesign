/**
 * LibRext组件配置
 */

// 默认样式
export const defaultStyle = {
  width: 100,
  height: 100,
  backgroundColor: '#ccc'
};

// 默认参数
export const defaultProps = {
  text: '这是一段文字'
};

export default {
  defaultStyle,
  defaultProps
};
