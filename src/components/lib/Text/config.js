/**
 * LibText组件配置
 */

// 默认样式
export const defaultStyle = {
  width: '200px',
  height: '100px',
  color: 'pink'
};

// 默认参数
export const defaultProps = {
  text: '这是一段文字'
};

export default {
  defaultStyle,
  defaultProps
};
